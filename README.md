# FastAPI web credentials example

```sh
uv sync
uv run fastapi dev --reload
```

log in with username `johndoe` and password `secret` to get the access token, then use the token to access the protected endpoint `/users/me`.
